---
/**
 * Hero Section - 首頁主視覺區塊（極簡版）
 */

import { siteConfig } from '../../config/siteConfig';

interface Props {
    greeting?: string;
    subtitle?: string;
}

const {
    greeting = '判斷力比執行力值錢。',
    subtitle = '工程師出身，靠架構思維跟 AI 協作，把技術翻譯成人話。',
} = Astro.props;
---

<section class="hero">
    <div class="hero-content">
        <h1 class="hero-title">{greeting}</h1>
        <p class="hero-subtitle">{subtitle}</p>
        <div class="hero-buttons">
            <a href={siteConfig.servicesUrl} class="hero-cta hero-cta-primary">找到你的起點 &rarr;</a>
        </div>
        <p class="hero-secondary">
            還不確定？<a href={siteConfig.starterPackUrl}>先領免費實戰上手包</a> 或 <a href={siteConfig.threadsUrl} target="_blank" rel="noopener">來 Threads 逛逛</a>
        </p>
    </div>
</section>

<style>
    .hero {
        min-height: 60vh;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        padding: 8rem 2rem 4rem;
        background: var(--bg);
    }

    .hero-content {
        max-width: 640px;
    }

    .hero-title {
        font-family: 'Noto Serif TC', serif;
        font-size: clamp(2.2rem, 6vw, 3.5rem);
        font-weight: 700;
        line-height: 1.3;
        color: var(--text-primary);
        margin-bottom: 1.25rem;
        letter-spacing: -0.02em;
    }

    .hero-subtitle {
        font-size: 1.1rem;
        color: var(--text-secondary);
        line-height: 1.7;
        margin-bottom: 2.5rem;
    }

    .hero-buttons {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
        margin-bottom: 1.25rem;
    }

    .hero-cta {
        display: inline-block;
        padding: 0.85rem 2.5rem;
        font-size: 1rem;
        font-weight: 600;
        border-radius: 10px;
        text-decoration: none;
        transition: all 0.2s ease;
    }

    .hero-cta-primary {
        color: white;
        background: var(--accent);
        border: 1px solid var(--accent);
    }

    .hero-cta-primary:hover {
        background: var(--accent-light);
        border-color: var(--accent-light);
        transform: translateY(-2px);
        box-shadow: var(--shadow-md);
    }

    .hero-secondary {
        font-size: 0.85rem;
        color: var(--text-muted);
        margin: 0;
    }

    .hero-secondary a {
        color: var(--text-secondary);
        text-decoration: underline;
        text-underline-offset: 2px;
        text-decoration-color: var(--border);
        transition: color 0.2s ease;
    }

    .hero-secondary a:hover {
        color: var(--accent);
        text-decoration-color: var(--accent);
    }

    @media (max-width: 768px) {
        .hero {
            min-height: auto;
            padding: 7rem 1.5rem 3rem;
        }

        .hero-subtitle {
            font-size: 1rem;
        }
    }
</style>
