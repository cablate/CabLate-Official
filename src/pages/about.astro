---
import BaseLayout from '../layouts/BaseLayout.astro';
import { ArrowRight, Github, AtSign } from '@lucide/astro';
import { authorityProfile, deliveryExperienceProofs, openSourceProofs } from '../config/authority';
import { siteConfig, withCabAiAttribution } from '../config/siteConfig';

const personJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'ProfilePage',
  dateModified: '2026-07-13',
  mainEntity: {
    '@type': 'Person',
    '@id': `${authorityProfile.authorUrl}#person`,
    name: authorityProfile.publicName,
    alternateName: authorityProfile.alternateName,
    url: authorityProfile.authorUrl,
    jobTitle: 'AI 應用講師／全端工程師',
    description: authorityProfile.shortBio,
    sameAs: authorityProfile.sameAs,
    knowsAbout: ['AI Agent', 'Claude Code', 'Vibe Coding', 'Context Engineering', 'Harness Engineering', 'Skill Design'],
  },
};

const timeline = [
  { time: '非本科轉職', title: '從前端開始，一路補到能把系統接起來', text: '我從前端進入工程，在實務專案裡補上後端、部署、系統整合與團隊協作。這段經歷讓我很早就知道，聽懂名詞和真的把事情完成，是兩回事。' },
  { time: '工程實務期', title: '開始把資料、權限與失敗處理一起算進成果', text: '進入政府與企業系統後，我開始承接系統分析與設計工作。功能能跑還不夠，資料、權限、驗收、異常處理與交接都必須一起考慮。' },
  { time: '2024', datetime: '2024', title: '把工程判斷帶進 AI 應用', text: '我開始把工程背景轉向 AI 應用，也協助外部團隊理解 AI 可以怎麼進入真實工作。原本只出現在工程專案裡的判斷方式，逐漸延伸到內容、教學與產品。' },
  { time: '2025-06 至今', datetime: '2025-06', title: '全職投入 CabLate', text: '2025 年 6 月離開正職後，我全職投入 CabLate，陸續發展課程、講座、付費手冊、CabAI 平台與顧問服務。形式不同，但核心一直是同一件事：讓複雜的方法不只被看懂，也能真的被拿去使用。' },
];

const [featuredOutput, ...supportingOutputs] = openSourceProofs;
---

<BaseLayout title="關於 CabLate｜從全端工程到 AI 工作流程" description="CabLate 從全端工程與系統設計出發，持續研究 AI 應用、Claude Code、Agent 工作流程與數位產品落地，並透過課程、開源工具、公開研究與顧問合作分享實作方法。">
  <script slot="head" type="application/ld+json" set:html={JSON.stringify(personJsonLd)} />

  <article class="about-page paper-page">
    <header class="about-hero page-paper paper-card">
      <div class="about-hero__topline">
        <p class="about-kicker">關於 CabLate</p>
        <span class="about-note">WORK NOTES / 01</span>
      </div>
      <div class="about-hero__grid">
        <div class="about-hero__headline">
          <h1 class="display-heading"><span class="heading-phrase">我從工程出發，</span><span class="heading-phrase">也因此很難把</span><span class="heading-phrase">「AI 成功做過一次」</span><span class="heading-phrase">當成真正完成</span></h1>
        </div>
        <div class="about-hero__aside">
          <p class="about-hero__lead">我是 CabLate，做過全端工程、系統設計與產品實作，現在把這些經驗用在 AI 教學、Agent 工作流程與數位產品上。</p>
          <p>比起展示 AI 可以多快產出，我更在意它放進真實工作後會不會失控，以及別人接手時能不能看懂發生了什麼。</p>
          <dl class="about-hero__facts">
            <div><dt>我處理的問題</dt><dd>AI 應用、工作流程、產品實作與系統整合</dd></div>
            <div><dt>我持續留下</dt><dd>開源工具、公開研究、課程與工程手冊</dd></div>
          </dl>
          <p class="about-hero__referral">你可以先看我做過的東西，再決定這套判斷是否適合你。</p>
          <a class="btn btn-ghost about-hero__action" href="#public-output-title">先看公開作品 <ArrowRight size={16} aria-hidden="true" /></a>
        </div>
      </div>
    </header>

    <section class="about-story page-paper paper-card" aria-labelledby="story-title">
      <div class="about-story__intro section-heading">
        <p class="about-kicker">我怎麼走到這裡</p>
        <h2 id="story-title">工程經驗讓我學會，功能能跑只是開始</h2>
        <span class="about-story__mark" aria-hidden="true">↳</span>
      </div>
      <div class="about-story__body">
        <p class="about-story__opening">剛開始做開發時，我也會把「功能寫完」當成完成。進入更複雜的系統後，才發現真正麻煩的往往發生在功能寫完之後。</p>
        <div class="about-story__copy">
          <p>資料可能不一致、權限可能漏掉、需求可能在交接時變形。即使畫面看起來正常，只要沒有人知道怎麼驗收、出了問題該查哪裡，它就很難成為可靠的成果。</p>
          <p>AI 把產出速度拉得更快，也把這些問題一起放大。現在做課程、產品或合作時，我仍然會回頭看：AI 拿到了什麼、用了什麼、做對的原因有沒有被留下，以及最後誰能確認它真的完成。</p>
        </div>
      </div>
    </section>

    <section class="turning-points page-paper paper-card paper-card--clip" aria-labelledby="timeline-title">
      <header class="turning-points__header">
        <div class="section-heading">
          <p class="about-kicker">關鍵轉折</p>
          <h2 id="timeline-title">四段經歷，慢慢形成現在的工作方式</h2>
        </div>
        <p class="turning-points__note">從非本科轉職、工程實務，到把重心轉向 AI 應用，這些經歷共同塑造了我現在看問題的方法。</p>
      </header>
      <ol class="timeline">
        {timeline.map((item, index) => (
          <li>
            <div class="timeline__rail" aria-hidden="true"><span class="timeline__node">0{index + 1}</span></div>
            <div class="timeline__meta"><time datetime={item.datetime}>{item.time}</time></div>
            <div class="timeline__content"><h3>{item.title}</h3><p>{item.text}</p></div>
          </li>
        ))}
      </ol>
    </section>

    <div class="trust-evidence page-paper paper-card">
      <section class="public-output" aria-labelledby="public-output-title">
        <header class="public-output__header section-heading">
          <p class="about-kicker">公開作品</p>
          <h2 id="public-output-title">先看我做過什麼，<br />再決定要不要相信我</h2>
          <p>我盡量把工具、研究與方法留在可以查證的地方。你可以直接看程式碼、文件與更新紀錄，而不是只看一段自我介紹。</p>
        </header>

        <article class="public-output__featured">
          <div class="public-output__featured-title">
            <p class="public-output__category">{featuredOutput.kind}</p>
            <h3>{featuredOutput.headline}</h3>
          </div>
          <div class="public-output__featured-copy">
            <p>{featuredOutput.description}</p>
            <p class="public-output__capability">{featuredOutput.capability}</p>
            <div class="public-output__source-row">
              <a class="public-output__source-link" href={featuredOutput.href} target="_blank" rel="noopener">
                <span>{featuredOutput.sourceLabel}</span><span class="visually-hidden">：{featuredOutput.title}</span>
                <ArrowRight size={15} aria-hidden="true" />
              </a>
            </div>
          </div>
        </article>

        <div class="public-output__supporting">
          {supportingOutputs.map((output) => (
            <article>
              <div class="public-output__identity">
                <p class="public-output__category">{output.kind}</p>
                <span>{output.title}</span>
              </div>
              <h3>{output.headline}</h3>
              <p>{output.description}</p>
              <p class="public-output__capability">{output.capability}</p>
              <div class="public-output__source-row">
                <a class="public-output__source-link" href={output.href} target="_blank" rel="noopener">
                  <span>{output.sourceLabel}</span><span class="visually-hidden">：{output.title}</span>
                  <ArrowRight size={15} aria-hidden="true" />
                </a>
              </div>
            </article>
          ))}
        </div>

        <div class="trust-evidence__channels" id="public-output-actions">
          <p>繼續看公開輸出</p>
          <ul role="list">
            <li><a class="btn btn-ghost" href={siteConfig.githubUrl} target="_blank" rel="noopener"><Github size={17} aria-hidden="true" /><span>GitHub 個人頁</span><ArrowRight size={15} aria-hidden="true" /></a></li>
            <li><a class="btn btn-ghost" href={siteConfig.threadsUrl} target="_blank" rel="noopener"><AtSign size={17} aria-hidden="true" /><span>Threads 帳號</span><ArrowRight size={15} aria-hidden="true" /></a></li>
          </ul>
        </div>
      </section>

      <section class="delivery-experience" aria-labelledby="delivery-experience-title">
        <header class="delivery-experience__header section-heading">
          <p class="about-kicker">課程與內容</p>
          <h2 id="delivery-experience-title">除了做工具，<br />我也把方法整理成別人可以學會的內容</h2>
          <p>課程、教材與內容平台是另一種成果。重點不只是講懂概念，而是讓學員知道下一步怎麼做，以及出錯時可以回到哪裡查。</p>
        </header>
        <div class="delivery-experience__body">
          <div class="delivery-experience__list">
            {deliveryExperienceProofs.map((proof) => (
              <article>
                <p class="delivery-experience__kind">{proof.kind}</p>
                <div>
                  <h3>{proof.headline}</h3>
                  <p>{proof.description}</p>
                  <small>{proof.proof}</small>
                </div>
              </article>
            ))}
          </div>
          <article class="platform-proof" id="cabai-platform">
            <div>
              <p class="platform-proof__label">內容平台</p>
              <h3>從免費試看到已購內容，都留在同一個 CabAI 帳號裡</h3>
              <p>訪客可以先免費試看，購買後再用同一個帳號閱讀課程、手冊與後續更新。內容不會散落在不同信件、連結與平台裡。</p>
            </div>
            <a class="btn btn-ghost platform-proof__action" href={withCabAiAttribution(siteConfig.cabAi.homeUrl, 'about_platform')} target="_blank" rel="noopener">查看 CabAI 學習平台 <ArrowRight size={16} aria-hidden="true" /></a>
          </article>
        </div>
      </section>
    </div>

    <section class="principles page-paper paper-card paper-card--clip paper-card--clip-left" aria-labelledby="principles-title">
      <div class="principles__grid">
        <div class="principles__intro section-heading">
          <p class="about-kicker">工作原則</p>
          <h2 id="principles-title">工具會一直換，但這四件事不會</h2>
        <p class="principles__note">不論做研究、教學、產品或合作，我都會用下面四個原則確認自己沒有被熱門名詞帶著走。</p>
        </div>
        <div class="principles__list">
          <article><span>01</span><h3>沒有做過的，不假裝熟悉</h3><p>我會區分實際經驗、研究判斷與仍待驗證的想法，也會說清楚方法適用到哪裡。</p></article>
          <article><span>02</span><h3>先做出能被真實使用的版本</h3><p>讓使用者、限制與錯誤提早出現，再根據實際證據修正。</p></article>
          <article><span>03</span><h3>先看問題，再決定要不要用 AI</h3><p>簡單方法已經能穩定解決，就不為了 Agent、MCP 或自動化硬加複雜度。</p></article>
          <article><span>04</span><h3>做對一次，就把真正有效的原因留下來</h3><p>不只保存最後的 Prompt，也保存判斷、順序、例外與檢查方式。</p></article>
        </div>
      </div>
    </section>

    <section class="about-next page-paper paper-card paper-card--clip" id="next" aria-labelledby="next-title">
      <div>
        <p class="about-kicker">下一步</p>
          <h2 id="next-title">先從最接近你目前問題的<br />地方開始</h2>
        <p class="about-next__intro">想自己學，可以看課程與手冊；手上的問題已經影響工作，也可以直接把情境交給我一起釐清。</p>
      </div>
      <div class="about-next__links">
        <a class="btn btn-primary about-next__primary" href="/services/">了解服務與合作方式 <ArrowRight size={18} aria-hidden="true" /></a>
        <a class="btn btn-ghost about-next__secondary-action" href="/courses/">看學習內容 <ArrowRight size={18} aria-hidden="true" /></a>
      </div>
    </section>
  </article>
</BaseLayout>

<style>
  .about-page { padding-top: 0; }
  .about-page > .page-paper { overflow: visible; }
  .about-kicker { margin: 0 0 1rem; color: var(--accent); font-size: .72rem; font-weight: 700; letter-spacing: .1em; }
  .about-note { color: var(--text-muted); font-family: var(--font-mono); font-size: .62rem; letter-spacing: .08em; }
  .about-hero { padding-block: clamp(3.25rem, 7vw, 6.5rem) !important; }
  .about-hero__topline { display: flex; align-items: center; justify-content: space-between; gap: 1rem; padding-bottom: 1.35rem; border-bottom: 1px solid var(--border); }
  .about-hero__topline p { margin: 0; }
  .about-hero__grid { display: grid; grid-template-columns: minmax(0, 1.08fr) minmax(16rem, .92fr); gap: clamp(2.5rem, 7vw, 7rem); align-items: end; padding-top: clamp(2.5rem, 5vw, 4.75rem); }
  .about-hero__headline { container-type: inline-size; }
  .about-hero h1 { max-width: none !important; margin: 0; font-family: var(--font-sans); font-size: clamp(2rem, 4vw, 3.25rem) !important; letter-spacing: -.07em !important; line-height: 1.3 !important; }
  .about-hero__aside { max-width: 31rem; padding-bottom: .35rem; }
  .about-hero__aside p { margin: 0; color: var(--text-secondary); line-height: 1.85; }
  .about-hero__aside .about-hero__lead { margin-bottom: 1.2rem; color: var(--text-primary); font-size: 1.1rem; font-weight: 700; line-height: 1.65; }
  .about-hero__facts { display: grid; gap: .75rem; margin: 2rem 0 0; padding-top: 1rem; border-top: 1px solid var(--border); }
  .about-hero__facts div { display: grid; grid-template-columns: 5rem 1fr; gap: .8rem; }
  .about-hero__facts dt { color: var(--accent); font-size: .68rem; font-weight: 700; }
  .about-hero__facts dd { margin: 0; color: var(--text-secondary); font-size: .82rem; }
  .about-hero__referral { margin-top: 1.2rem !important; font-size: .82rem; line-height: 1.75 !important; }
  .about-hero__action { margin-top: 1rem; }
  .about-story { display: grid; grid-template-columns: minmax(12rem, .72fr) minmax(0, 1.28fr); gap: clamp(2.5rem, 8vw, 8rem); padding-block: clamp(3.5rem, 8vw, 7rem) !important; }
  .section-heading h2 { max-width: 16ch; margin: 0; font-size: clamp(1.6rem, 2.8vw, 2.4rem); letter-spacing: -.055em; line-height: 1.3; }
  .about-story__intro { position: relative; }
  .about-story__mark { position: absolute; right: 0; bottom: -.2rem; color: var(--accent); font-family: var(--font-serif); font-size: 3.2rem; line-height: 1; transform: rotate(-14deg); }
  .about-story__body { max-width: 45rem; }
  .about-story__opening { margin: 0 0 1.65rem; color: var(--text-primary) !important; font-size: clamp(1.35rem, 2.2vw, 1.9rem) !important; font-weight: 700; letter-spacing: -.025em; line-height: 1.55 !important; }
  .about-story__copy { display: grid; gap: 1.2rem; padding-top: 1.25rem; border-top: 1px solid var(--border); }
  .about-story__copy p { margin: 0; color: var(--text-secondary); line-height: 1.9; }
  .turning-points { padding-block: clamp(3.5rem, 7vw, 6.5rem) !important; }
  .turning-points__header { display: grid; grid-template-columns: minmax(0, 1fr) minmax(15rem, .65fr); gap: 2rem; align-items: end; padding-bottom: 2.5rem; border-bottom: 1px solid var(--border); }
  .turning-points__note { max-width: 24rem; margin: 0; color: var(--text-secondary); font-size: .86rem; line-height: 1.8; }
  .timeline { position: relative; margin: 0; padding: .4rem 0 0; list-style: none; }
  .timeline::before { content: ''; position: absolute; top: 1rem; bottom: 1.6rem; left: 1rem; width: 1px; background: color-mix(in srgb, var(--accent) 60%, var(--border)); }
  .timeline li { position: relative; display: grid; grid-template-columns: 2rem minmax(8rem, .42fr) minmax(0, 1.58fr); gap: 1.25rem; padding: 1.75rem 0; border-bottom: 1px solid var(--border); }
  .timeline li:last-child { border-bottom: 0; padding-bottom: .35rem; }
  .timeline__rail { position: relative; z-index: 1; }
  .timeline__node { display: grid; place-items: center; width: 2rem; height: 2rem; border: 1px solid var(--accent); border-radius: 50%; background: var(--bg-soft); color: var(--accent); font-family: var(--font-mono); font-size: .58rem; }
  .timeline__meta { padding-top: .25rem; }
  .timeline time { color: var(--accent); font-family: var(--font-mono); font-size: .68rem; line-height: 1.55; }
  .timeline__content { min-width: 0; }
  .timeline h3 { margin: 0 0 .55rem; font-size: 1.35rem; letter-spacing: -.025em; }
  .timeline li p { max-width: 47rem; margin: 0; color: var(--text-secondary); line-height: 1.8; }
  .trust-evidence { padding-block: clamp(3.5rem, 7vw, 6.5rem) !important; }
  #public-output-title, #public-output-actions, #delivery-experience-title { scroll-margin-top: 5.5rem; }
  .public-output__header { max-width: 48rem; padding-bottom: clamp(2.5rem, 5vw, 4rem); }
  .public-output__header h2 { max-width: 14em; }
  .public-output__header > p:last-child { max-width: 42rem; margin: 1.35rem 0 0; color: var(--text-secondary); font-size: .9rem; line-height: 1.85; }
  .public-output__featured { display: grid; grid-template-columns: minmax(14rem, .82fr) minmax(0, 1.18fr); gap: clamp(2rem, 6vw, 5rem); padding: clamp(2rem, 4vw, 3.5rem) 0; border-top: 1px solid var(--border-strong); border-bottom: 1px solid var(--border); }
  .public-output__featured-title, .public-output__featured-copy { min-width: 0; }
  .public-output__category { margin: 0; color: var(--accent); font-size: .7rem; font-weight: 700; letter-spacing: .08em; }
  .public-output__featured h3 { max-width: 18ch; margin: 1rem 0 0; font-family: var(--font-sans); font-size: clamp(1.4rem, 2.4vw, 2rem); letter-spacing: -.04em; line-height: 1.3; }
  .public-output__featured-copy > p:first-child { margin: 0; color: var(--text-primary); font-size: 1rem; line-height: 1.9; }
  .public-output__capability { margin: 1.2rem 0 0; padding-left: 1rem; border-left: 2px solid var(--accent); color: var(--text-secondary); font-size: .82rem; line-height: 1.75; }
  .public-output__source-row { display: flex; align-items: end; justify-content: space-between; gap: 1.25rem; margin-top: 1.6rem; padding-top: 1.1rem; border-top: 1px solid var(--border); }
  .public-output__source-link { display: inline-flex; flex: 0 0 auto; align-items: center; gap: .45rem; min-height: 2.75rem; color: var(--accent-text); font-size: .76rem; font-weight: 700; }
  .public-output__source-link:hover { color: var(--accent); }
  .public-output__supporting { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .public-output__supporting article { min-width: 0; padding: clamp(2rem, 4vw, 3rem) 0; border-bottom: 1px solid var(--border); }
  .public-output__supporting article:first-child { padding-right: clamp(1.5rem, 4vw, 3.25rem); border-right: 1px solid var(--border); }
  .public-output__supporting article:last-child { padding-left: clamp(1.5rem, 4vw, 3.25rem); }
  .public-output__identity { display: flex; flex-wrap: wrap; align-items: baseline; gap: .35rem .8rem; }
  .public-output__identity > span { color: var(--text-muted); font-family: var(--font-mono); font-size: .64rem; overflow-wrap: anywhere; }
  .public-output__supporting h3 { max-width: 22ch; margin: 1.1rem 0 .8rem; font-size: clamp(1.2rem, 2vw, 1.55rem); letter-spacing: -.025em; line-height: 1.45; }
  .public-output__supporting article > p:not(.public-output__category, .public-output__capability) { margin: 0; color: var(--text-secondary); font-size: .84rem; line-height: 1.8; }
  .public-output__supporting .public-output__source-row { align-items: flex-start; flex-direction: column; gap: .45rem; }
  /* Public profile links stay visible as buttons without competing with the page's final CTA. */
  .trust-evidence__channels { box-sizing: border-box; display: grid; grid-template-columns: minmax(10rem, .55fr) minmax(0, 1.45fr); gap: clamp(1.5rem, 4vw, 3rem); align-items: center; margin-top: 2rem; padding: 1.25rem; border: 1px solid var(--border-strong); border-radius: var(--radius-sm); background: color-mix(in srgb, var(--accent) 3%, var(--bg-soft)); }
  .trust-evidence__channels > p { margin: 0; color: var(--text-secondary); font-size: .82rem; font-weight: 700; letter-spacing: .02em; }
  .trust-evidence__channels ul { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: .75rem; margin: 0; padding: 0; list-style: none; }
  .trust-evidence__channels li { min-width: 0; }
  .trust-evidence__channels a { display: grid; grid-template-columns: auto 1fr auto; gap: .6rem; align-items: center; width: 100%; min-height: 3.25rem; padding: .75rem .9rem; border-color: var(--border-strong); background: var(--bg-soft); color: var(--text-primary); font-size: .78rem; font-weight: 700; }
  .trust-evidence__channels a:hover:not(:disabled):not([aria-disabled='true']) { border-color: var(--accent-text); color: var(--accent-text); }
  .delivery-experience { display: grid; grid-template-columns: minmax(13rem, .72fr) minmax(0, 1.28fr); gap: clamp(2.5rem, 7vw, 7rem); margin-top: clamp(4.5rem, 9vw, 8rem); padding-top: clamp(3rem, 6vw, 5rem); border-top: 1px solid var(--border-strong); }
  .delivery-experience__header h2 { max-width: 13em; }
  .delivery-experience__header > p:last-child { max-width: 28rem; margin: 1.35rem 0 0; color: var(--text-secondary); font-size: .86rem; line-height: 1.85; }
  .delivery-experience__body { min-width: 0; }
  .delivery-experience__list { border-top: 1px solid var(--border-strong); }
  .delivery-experience__list article { display: grid; grid-template-columns: minmax(7rem, .38fr) minmax(0, 1fr); gap: 1.5rem; padding: 1.7rem 0; border-bottom: 1px solid var(--border); }
  .delivery-experience__kind { margin: .15rem 0 0; color: var(--accent); font-size: .7rem; font-weight: 700; letter-spacing: .06em; }
  .delivery-experience__list h3 { margin: 0; font-size: 1.12rem; letter-spacing: -.02em; line-height: 1.5; }
  .delivery-experience__list article div > p { margin: .65rem 0 0; color: var(--text-secondary); font-size: .82rem; line-height: 1.75; }
  .delivery-experience__list small { display: block; margin-top: .85rem; color: var(--text-muted); font-family: var(--font-mono); font-size: .64rem; line-height: 1.55; }
  .platform-proof { display: grid; grid-template-columns: minmax(0, 1fr); gap: 1.25rem; margin-top: 2rem; padding: clamp(1.35rem, 3vw, 2rem); border: 1px solid var(--border); border-radius: var(--radius-sm); background: color-mix(in srgb, var(--accent) 4%, var(--bg-soft)); }
  .platform-proof__label { margin: 0 0 .65rem; color: var(--accent); font-size: .7rem; font-weight: 700; letter-spacing: .06em; }
  .platform-proof h3 { max-width: 34rem; margin: 0; font-size: clamp(1.25rem, 2.2vw, 1.7rem); line-height: 1.45; }
  .platform-proof p:last-child { max-width: 42rem; margin: .75rem 0 0; color: var(--text-secondary); font-size: .82rem; line-height: 1.75; }
  .platform-proof > a { justify-self: start; min-height: 3rem; white-space: nowrap; }
  .principles { padding: clamp(3.5rem, 7vw, 6.5rem) !important; }
  .principles__grid { display: grid; grid-template-columns: minmax(12rem, .72fr) minmax(0, 1.28fr); gap: clamp(2.5rem, 7vw, 7rem); }
  .principles__note { max-width: 20rem; margin: 1.5rem 0 0; color: var(--text-secondary); font-size: .84rem; line-height: 1.8; }
  .principles__list { display: grid; grid-template-columns: 1fr 1fr; border-top: 1px solid var(--border); border-left: 1px solid var(--border); }
  .principles article { min-height: 14rem; padding: 1.35rem; border-right: 1px solid var(--border); border-bottom: 1px solid var(--border); }
  .principles article span { color: var(--accent); font-family: var(--font-mono); font-size: .66rem; }
  .principles article h3 { margin: 2.75rem 0 .65rem; font-size: 1.12rem; }
  .principles article p { margin: 0; color: var(--text-secondary); font-size: .82rem; line-height: 1.75; }
  .about-next { display: grid; grid-template-columns: minmax(0, 1.1fr) minmax(15rem, .9fr); gap: clamp(2.5rem, 7vw, 7rem); padding-block: clamp(3.5rem, 7vw, 6rem) !important; }
  .about-next h2 { max-width: 17ch; margin: 0; font-size: clamp(1.6rem, 2.8vw, 2.4rem); letter-spacing: -.055em; line-height: 1.3; }
  .about-next__intro { max-width: 30rem; margin: 1.35rem 0 0; color: var(--text-secondary); line-height: 1.8; }
  .about-next__links { align-self: end; border-top: 1px solid var(--border-strong); }
  .about-next__links > a { display: flex; align-items: center; justify-content: space-between; gap: 1rem; min-height: 3.25rem; width: 100%; padding: .75rem 1rem; border-bottom: 1px solid var(--border); font-weight: 700; }
  .about-next__links > a.about-next__primary {
    width: 100%;
    min-height: 3.25rem;
    border: 1px solid var(--text-primary);
    border-radius: var(--radius-sm);
    background: var(--text-primary);
    color: #fff;
    padding: .75rem 1rem;
  }
  .about-next__links > a.about-next__primary:hover:not(:disabled):not([aria-disabled='true']) {
    border-color: var(--accent-text);
    background: var(--accent-text);
    color: #fff;
  }
  .about-next__links > a.about-next__secondary-action { margin-top: .75rem; border: 1px solid var(--border-strong); border-radius: var(--radius-sm); background: transparent; color: var(--text-primary); }
  .about-next__links > a.about-next__secondary-action:hover:not(:disabled):not([aria-disabled='true']) { border-color: var(--accent-text); background: var(--bg-soft); color: var(--accent-text); }
  @media (max-width: 800px) {
    .about-hero__grid, .about-story, .turning-points__header, .public-output__featured, .delivery-experience, .principles__grid, .about-next { grid-template-columns: 1fr; }
    .about-hero__grid { gap: 2.25rem; }
    .about-hero__aside { max-width: 38rem; }
    .about-hero__action, .platform-proof__action { width: 100%; }
    .about-story__mark { right: 1rem; }
    .turning-points__header { gap: 1.25rem; }
    .public-output__featured { gap: 1.75rem; }
    .public-output__supporting { grid-template-columns: 1fr; }
    .public-output__supporting article:first-child { padding-right: 0; border-right: 0; }
    .public-output__supporting article:last-child { padding-left: 0; }
    .delivery-experience { gap: 2.5rem; }
    .principles__list { grid-template-columns: 1fr 1fr; }
  }
  @media (max-width: 560px) {
    .about-page.paper-page > .page-paper { padding-inline: clamp(1rem, 5vw, 1.25rem) !important; }
    .about-page .display-heading > .heading-phrase { white-space: normal; }
    .about-hero { padding-block: 2.4rem 3rem !important; }
    .about-hero__topline { padding-bottom: 1rem; }
    .about-note { font-size: .55rem; }
    .about-hero__grid { padding-top: 2.15rem; }
    .about-hero h1 { font-size: clamp(1.9rem, 8vw, 2.6rem) !important; line-height: 1.3 !important; }
    .about-hero__aside .about-hero__lead { font-size: 1rem; }
    .about-hero__facts { margin-top: 1.35rem; }
    .about-story, .turning-points, .trust-evidence, .principles, .about-next { padding-block: 2.8rem !important; }
    .about-story__opening { font-size: 1.25rem !important; }
    .about-story__mark { bottom: -.5rem; font-size: 2.6rem; }
    .turning-points__header { padding-bottom: 1.7rem; }
    .timeline::before { top: 1rem; bottom: 1.2rem; left: .85rem; }
    .timeline__rail { align-self: stretch; }
    .timeline li { grid-template-columns: 1.7rem minmax(0, 1fr); column-gap: .85rem; padding: 1.25rem 0; }
    .timeline__rail { grid-column: 1; grid-row: 1 / span 2; }
    .timeline__node { width: 1.7rem; height: 1.7rem; font-size: .52rem; }
    .timeline__meta { grid-column: 2; grid-row: 1; padding-top: .05rem; }
    .timeline__content { grid-column: 2; grid-row: 2; }
    .timeline h3 { margin-top: .35rem; font-size: 1.15rem; }
    .timeline li p { font-size: .84rem; line-height: 1.75; }
    .public-output__header { padding-bottom: 2rem; }
    .public-output__header > p:last-child { font-size: .84rem; }
    .public-output__featured { padding: 1.7rem 0; }
    .public-output__featured h3 { font-size: 1.65rem; }
    .public-output__source-row { align-items: flex-start; flex-direction: column; gap: .5rem; }
    .public-output__supporting article { padding: 1.75rem 0; }
    .public-output__supporting h3 { font-size: 1.18rem; }
    .trust-evidence__channels { grid-template-columns: 1fr; gap: 1rem; }
    .trust-evidence__channels ul { grid-template-columns: 1fr; }
    .delivery-experience { margin-top: 4rem; padding-top: 3rem; }
    .delivery-experience__list article { grid-template-columns: 1fr; gap: .7rem; padding: 1.45rem 0; }
    .delivery-experience__kind { margin: 0; }
    .platform-proof { padding: 1.25rem; }
    .platform-proof > a { width: 100%; white-space: normal; }
    .principles__list { grid-template-columns: 1fr; }
    .principles article { min-height: auto; padding: 1.25rem; }
    .principles article h3 { margin-top: 1.8rem; }
  }
</style>
